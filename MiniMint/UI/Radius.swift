import SwiftUI

extension MintyUI {
  // MARK: - Radius

  /// Corner-radius tokens shared across the app's surfaces.
  enum Radius {

    // MARK: Internal

    /// The standard rounding for cards, sheets, and other surfaces.
    static let standard: CGFloat = 14
  }
}
